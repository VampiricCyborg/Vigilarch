# Rust
/target/
**/*.rs.bk
*.pdb

# WASM / node
node_modules/
dist/
.vite/
*.wasm

# wasm-bindgen output for the demo page. Generated by web/build.sh from the
# workspace; checking in half of it (the .js glue, with the .wasm ignored above)
# would leave a broken artifact in the tree.
web/pkg/

# Local node state — never commit a ledger or a key
*.sqlite
*.sqlite-wal
*.sqlite-shm
/blobs/
*.key
*.pem
!testdata/**/*.key

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# The operating contract is local-only: it is instructions to a coding agent,
# not part of the published artifact. Ignored case-insensitively so that both
# CLAUDE.md and claude.md are covered.
CLAUDE.md
claude.md
running.md